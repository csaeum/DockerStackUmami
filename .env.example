# ============================================
# Umami Analytics Stack - Environment Config
# ============================================

# Project Settings
# ----------------
COMPOSE_PROJECT_NAME=umami

# Domain & Routing
# ----------------
# Beispiel: Host(`analytics.example.com`) || Host(`umami.example.com`)
# Multiple Domains mit || trennen
HOSTRULE=Host(`analytics.example.com`)

# Database Configuration
# ----------------------
DB_NAME=umami
DB_USER=umami
# Sichere Passwörter generieren (URL-sicher, ohne Sonderzeichen: openssl rand -hex 32)
DB_PASSWORD=

# Application Secret
# ------------------
# WICHTIG: Generiere einen sicheren zufälligen String (mindestens 32 Zeichen)
# URL-sicher, ohne Sonderzeichen: openssl rand -hex 48
APP_SECRET=

# Backup Configuration
# --------------------
# Absoluter Pfad zum Backup-Verzeichnis auf dem Host
BACKUPVOLUME=/pfad/zu/ihrem/backup

# Network Configuration
# ---------------------
# Name des externen Traefik Proxy-Netzwerks
PROXY_NETWORK=traefik_proxy_network

# Timezone
# --------
TIMEZONE=Europe/Berlin
